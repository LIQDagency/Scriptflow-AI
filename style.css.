body {
  margin: 0;
  font-family: "Segoe UI", sans-serif;
  background-color: #0e0e0e;
  color: #f1f1f1;
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100vh;
}

.container {
  width: 90%;
  max-width: 600px;
  background-color: #1a1a1a;
  padding: 2rem;
  border-radius: 16px;
  box-shadow: 0 0 20px rgba(255, 255, 255, 0.05);
  transition: all 0.3s ease-in-out;
}

.title {
  text-align: center;
  font-size: 2rem;
  margin-bottom: 1.5rem;
}

form {
  display: flex;
  flex-direction: column;
}

input[type="text"] {
  padding: 0.75rem;
  border: none;
  border-radius: 8px;
  margin-bottom: 1rem;
  background-color: #2e2e2e;
  color: #fff;
}

button {
  padding: 0.75rem;
  border: none;
  border-radius: 8px;
  background: linear-gradient(135deg, #00f5a0, #00d9f5);
  color: black;
  font-weight: bold;
  cursor: pointer;
  transition: background 0.3s ease-in-out;
}

button:hover {
  background: linear-gradient(135deg, #00c3ff, #00f5a0);
}

.result {
  margin-top: 2rem;
  background-color: #2a2a2a;
  padding: 1rem;
  border-radius: 12px;
  white-space: pre-wrap;
}
